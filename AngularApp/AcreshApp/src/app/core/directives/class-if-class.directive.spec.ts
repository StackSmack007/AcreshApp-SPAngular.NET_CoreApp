import { ClassIfClassDirective } from './class-if-class.directive';

describe('ClassIfClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassIfClassDirective();
    expect(directive).toBeTruthy();
  });
});
